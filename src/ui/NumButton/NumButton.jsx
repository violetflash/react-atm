import React from 'react';

import { NumButtonContainer } from '../../styles/styles';

export const NumButton = ({ text }) => {
    return (
        <NumButtonContainer>{text}</NumButtonContainer>
    );
};
